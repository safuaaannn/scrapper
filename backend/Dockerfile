FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV BROWSER_SERVICE_URL=http://browser:3000
ENV PORT=8000

EXPOSE 8000

CMD ["gunicorn", "app:app", "-b", "0.0.0.0:8000", "-w", "2", "--timeout", "120"]
